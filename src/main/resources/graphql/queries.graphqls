type Query {
    getAllBooks(filter: BookFilter!): [Book]
    getBookById(id: ID!): Book
}

type Mutation {
    createNewBook(book: NewBook!): ID
}

type Subscription {
    getNewBooksInRealTime: Book
    getMockBooksEverySecond: Book
}
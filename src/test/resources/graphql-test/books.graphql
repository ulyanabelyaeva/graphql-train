query GetAllBooks {
    getAllBooks(filter: { pageNumber: 0, pageSize: 10 }) {
        name
        pageCount
        genres {
            id
            name
        }
        author {
            id
            name
        }
    }
}

mutation CreateNewBook {
    createNewBook(book: { name: "Dune", pageCount: 1500 })
}